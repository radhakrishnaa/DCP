'use strict';

/* jasmine specs for controllers go here */

describe('Portal Controllers', function(){
  beforeEach(module('upgrades.controllers'));

  it('should return a valid upgrade path', inject(function() {
    //spec body
  }));

  it('should ....', inject(function() {
    //spec body
  }));
});
